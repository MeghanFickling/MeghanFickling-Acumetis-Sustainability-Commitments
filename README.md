# Acumetis Sustainability Commitments

Upload `index.html`, `styles.css`, and `app.js` to the root of the repository.

Then enable GitHub Pages under **Settings → Pages → Deploy from a branch → main → /(root)**.

Before publishing, replace `$X` in `index.html` with the confirmed 2025 charitable donation amount.
